name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Build & release (Windows)
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - run: npm ci

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Pitlane ${{ github.ref_name }}"
          releaseBody: |
            See the assets below to install Pitlane on Windows.

            **NSIS installer** (recommended): `Pitlane_x.y.z_x64-setup.exe`
            **MSI**: `Pitlane_x.y.z_x64_en-US.msi`
          releaseDraft: false
          prerelease: false
